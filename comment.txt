Even if 
Meta name: [X-Parsed-By], Meta value: [org.apache.tika.parser.DefaultParser]
the class of parser is actually AutoDetectParser 
because AutoDetectParser is a subclass of rg.apache.tika.parser.CompositeParser

which

Composite parser that delegates parsing tasks to a component parser based on the declared content type of the incoming document. A fallback parser is defined for cases where a parser for the given content type is not available.
(https://tika.apache.org/1.13/api/org/apache/tika/parser/CompositeParser.html)



Fallback:

Fallback: org.apache.tika.parser.EmptyParser

Dummy parser that always produces an empty XHTML document without even attempting to parse the given document stream. Useful as a sentinel parser for unknown document types.
(https://tika.apache.org/1.13/api/org/apache/tika/parser/EmptyParser.html)

---
Force application/pdf to be processed by EmptyParser
--> both meta for X-Parsed-By changed to  org.apache.tika.parser.CompositeParser from org.apache.tika.parser.DefaultParser



Find tessdata
https://community.dur.ac.uk/nick.white/grctraining/desktop.html
ls /usr/local/Cellar/tesseract/3.04.01_2/share/tessdata/

Alpha channel in the test Tiff..?

http://stackoverflow.com/questions/5083492/tesseract-and-tiff-format-spp-not-in-set-1-3
convert testTiff.tiff -background white -flatten +matte testTiff_noAlpha.tiff

If we use TesseractOCRParser in the code...
A strange thing is: we still get EmptyParser in the meta (and nothing else) after OCR is successful
Meta name: [X-Parsed-By], Meta value: [org.apache.tika.parser.EmptyParser]

If we use AutoDetectParser with proper TIFF (no alpha?) as input - it does OCR automatically !?

----

If we use tika config to force using OCR parser to handle image/tiff, it indeed performs OCR (with meta too!)
But the parser name is still CompositeParser...

Summary?
It seems OCR is the default parser for image/tiff
But when it fails to extract content - we kind of do not know whether OCR was attempted, whether the input TIFF is not in the right format, or even if tesseract is installed and configured properly?
